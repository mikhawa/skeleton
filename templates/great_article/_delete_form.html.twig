<form method="post" action="{{ path('great_article_delete', {'id': great_article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ great_article.id) }}">
    <button class="btn">Delete</button>
</form>
