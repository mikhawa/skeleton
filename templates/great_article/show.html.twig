{% extends 'base.html.twig' %}

{% block title %}GreatArticle{% endblock %}

{% block body %}
    <h1>GreatArticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ great_article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ great_article.titre }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ great_article.texte }}</td>
            </tr>
            <tr>
                <th>Thedate</th>
                <td>{{ great_article.thedate ? great_article.thedate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('great_article_index') }}">back to list</a>

    <a href="{{ path('great_article_edit', {'id': great_article.id}) }}">edit</a>

    {{ include('great_article/_delete_form.html.twig') }}
{% endblock %}
